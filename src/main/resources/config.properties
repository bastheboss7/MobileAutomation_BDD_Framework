# =============================================================================
# Mobile Automation Framework Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Appium Server Configuration
# -----------------------------------------------------------------------------
HUB=http://127.0.0.1:4723
PORT=8203

# -----------------------------------------------------------------------------
# Platform Configuration (android or ios)
# -----------------------------------------------------------------------------
platform=ios

# -----------------------------------------------------------------------------
# Device Configuration - iOS
# -----------------------------------------------------------------------------
iosDeviceName=iPhone 16e
iosUdid=473154E6-C7B8-494F-A943-CAE9B3033BCC
iosPlatformVersion=26.2

# -----------------------------------------------------------------------------
# Application Configuration - iOS (WDIO Demo App)
# -----------------------------------------------------------------------------
iosAppPath=src/app/wdiodemoapp.app
bundleId=org.reactjs.native.example.wdiodemoapp
installApp=true

# -----------------------------------------------------------------------------
# Android Configuration (WDIO Demo App)
# -----------------------------------------------------------------------------
deviceName=emulator-5554
udid=emulator-5554
platformVersion=12
appPackage=com.wdiodemoapp
appActivity=com.wdiodemoapp.MainActivity
apkPath=src/app/android.wdio.native.app.v1.0.8.apk

# -----------------------------------------------------------------------------
# Timeouts (in seconds)
# -----------------------------------------------------------------------------
implicitWait=10
explicitWait=30
pageLoadTimeout=60

# -----------------------------------------------------------------------------
# Test Execution Settings
# -----------------------------------------------------------------------------
# Retry configuration for flaky tests
retry.maxCount=2

# Screenshot configuration
# on.step=true: Captures after EVERY step (includes passing steps - use for debugging only)
# on.failure=true: Captures when SCENARIO fails (essential for debugging)
screenshot.on.step=true
screenshot.on.failure=true
